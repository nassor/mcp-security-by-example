# Copy to .env (optional). The app falls back to this exact value if DATABASE_URL is unset,
# so the defaults below match compose.yaml and `cargo run` works with zero configuration.
DATABASE_URL=postgres://app:app@localhost:5432/appdb
